export interface Notification {
    id: string;
    event_type: string;
    message: string;
    created_at: string;
}

export interface NotificationWSMessage {
    id: string;
    event_type: string;
    message: string;
    created_at: string;
}
